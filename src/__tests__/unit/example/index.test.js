require('../config.test');
const { assert } = require('chai');

context('Example', () => {
  describe('Run Example', () => {
    it('should run example', async () => {
      assert.isTrue(true);
    });
  });
});
